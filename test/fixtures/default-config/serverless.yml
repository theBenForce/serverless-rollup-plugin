service: default-config

frameworkVersion: '4'

plugins:
  - ../../../dist # self-import
  - serverless-localstack

provider:
  name: aws
  runtime: nodejs20.x

functions:
  hello:
    handler: handler.hello
